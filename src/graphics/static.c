void staticDisplay(App* app)
{
    /*cvAddTexture(100 / 50 / 2, -800 / 50 / 2, app->textures.staticE[ app->textures.staticA ]);
    app->textures.staticA++;
    if (app->textures.staticA > 20)
        app->textures.staticA = 0;*/
}